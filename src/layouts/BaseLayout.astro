---
import "@fontsource/zen-kaku-gothic-new";
import Footer from "./Footer.astro";
const { title } = Astro.props;
---

<!doctype html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta
      name="description"
      content="FF14 サブマリンボイジャー時間管理ツールです。ログインしていなくても残り時間を可視化できます。今は潜水艦のみを設定できます。"
    />
    <title>{title}</title>
    <link rel="icon" href="/favicon.ico" />
    <script
      type="text/partytown"
      src="https://www.googletagmanager.com/gtag/js?id=G-JDR7J7W364"></script>
    <script type="text/partytown">
      window.dataLayer = window.dataLayer || [];
      function gtag() {
        dataLayer.push(arguments);
      }
      gtag("js", new Date());
      gtag("config", "G-JDR7J7W364");
    </script>
  </head>
  <body>
    <div class="container">
      <slot />
    </div>
    <Footer />
  </body>
</html>

<style>
  /*
  Josh's Custom CSS Reset
  https://www.joshwcomeau.com/css/custom-css-reset/
*/
  *,
  *::before,
  *::after {
    box-sizing: border-box;
  }
  * {
    margin: 0;
  }
  body {
    line-height: 1.5;
    -webkit-font-smoothing: antialiased;
  }
  img,
  picture,
  video,
  canvas,
  svg {
    display: block;
    max-width: 100%;
  }
  input,
  button,
  textarea,
  select {
    font: inherit;
  }
  p,
  h1,
  h2,
  h3,
  h4,
  h5,
  h6 {
    overflow-wrap: break-word;
  }
  #root,
  #__next {
    isolation: isolate;
  }
  /* ここまでリセットCSS */
  :root {
    --color-text: #4e4a4a;
    --color-link: #30869e;
    --color-white: #fff;
    --color-lightgray: #c5c2c2;
  }
  a {
    color: var(--color-link);
    text-decoration: underline;
  }
  a:hover {
    text-decoration: none;
  }
  body {
    font-family: "Zen Kaku Gothic New", sans-serif;
    display: flex;
    flex-direction: column;
    min-height: 100vh;
    color: var(--color-text);
  }
  .container {
    flex: 1;
    margin: 0 auto;
    max-width: 50rem;
    width: 90%;
  }
</style>
